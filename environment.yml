name: ripple_detection
channels:
- defaults
- conda-forge
dependencies:
- python >=3.10,<3.14
- "numpy >= 1.23.0"
- "scipy >= 1.9.0"
- "pandas >= 1.5.0"
- "matplotlib >= 3.5.0"
- "jupyter >= 1.0.0"
- "jupyterlab >= 3.0.0"
- "pytest >= 7.0.0"
- "pytest-cov >= 4.0.0"
- coveralls
- "ruff >=0.16,<0.17"
- pip
- pip:
  - hatchling
  - hatch-vcs