name: ripple_detection
channels:
- conda-forge
- defaults
dependencies:
- python >=3.10
- "numpy >= 1.24"
- "scipy >= 1.10"
- "pandas >= 2.0"
- "matplotlib >= 3.5.0"
- "jupyter >= 1.0.0"
- "jupyterlab >= 3.0.0"
- "pytest >= 7.0.0"
- "pytest-cov >= 4.0.0"
- "hypothesis >= 6.0.0"
- "pytest-snapshot >= 0.9.0"
- "ruff >=0.16,<0.17"
- "mypy >= 1.8.0"
- pip
- pip:
  - hatchling
  - hatch-vcs
