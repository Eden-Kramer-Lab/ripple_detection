# Gupta 2010 — Hippocampal Replay Is Not a Simple Function of Experience
Source: the extracted text (pdftotext of the Dropbox PDF, includes the Supplemental Experimental Procedures, PDF p. 21); title verified: yes (Gupta, van der Meer, Touretzky & Redish, Neuron 65:695–705).
Trigger: other (event windows come from a spike-sequence scoring algorithm; SWR power, ≥ 3 active cells and pausing at a reward site are gates on those windows)

## Method as implemented

Event windows (Supplemental Experimental Procedures, "Sequence detection", PDF p. 21):
- "Stepping through each session in 10 ms steps, the sequence score of place cell activity within a flexible time window (40 ms to 600 ms, to accommodate longer sequences) was calculated". Scores are pairwise comparisons of spike order versus place-field-centre order among "local" spikes ("within 80 ms of its neighbor and whose place field center was separated by less than 80 cm"). "The time window was allowed to keep increasing until there were five consecutive time window steps (50 ms) without an increase in the score or the window reached 600 ms." Run four times per session (left/right laps × forward/backward) (main text Experimental Procedures, p. 702–703).
- Signal: sorted place cells ("3088 place fields from the 2183 place cells"; putative interneurons excluded, p. 702).

Gates (Supplemental, "Sequence significance testing", PDF p. 21):
- SWR power: "SWR power was calculated at each point in time by averaging the SWR power from all hippocampal tetrodes (filtered between 180 Hz and 220 Hz) (Jackson et al., 2006). Only events occurring at reward sites with a SWR power greater than 2 standard deviations above the mean for the session were considered." How power is measured (Hilbert amplitude, squared, log) is deferred to Jackson 2006; which part of a window must exceed 2 SD (peak, any sample, mean) is not stated. Smoothing: not stated.
- Cells and behaviour: "17593 events across 31 sessions were identified in which at least three cells were active, the SWR power was above threshold, and the animal was paused at reward sites." "Paused" has no speed threshold in the text; the main text says replays were analysed "while animals were paused at reward sites" (p. 703).
- Duration: the window limits, 40–600 ms, are the only duration limits.
- Merging: "Because replays were initially identified for left and right laps separately, identical pairs (i.e. replay pairs occurring over the central stem) were merged." Other merging: not stated.
- No theta or sleep criterion (awake task).

Later analysis restrictions (not detection): two bootstrap shuffles (300 each; spike-time and cell-identity), "If the unshuffled replay score was greater than 95% of both independent sets of shuffled sequences, the replay was deemed significant"; "only events with 3 or more cells were included in the analyses"; 1719 significant of 17593. Bayesian decoding (20 ms window) of the identified replay times is a control.

## Inherited from
Jackson, Johnson & Redish 2006 (J Neurosci 26:12415; PMC6674885, read via WebFetch, quotes as returned by the page summarizer): "SWR events were extracted by down-sampling the LFP traces by a factor of 2 ... and bandpass filtering from 100 to 250 Hz. Amplitude for each trace was found via Hilbert-transform and then averaged across traces. The distribution of log-transformed average amplitude was used to find samples >2.5σ from the mean power." So "SWR power" in Gupta is most likely the Hilbert amplitude averaged across tetrodes, possibly log-transformed (inference; Gupta changes the band to 180–220 Hz and the threshold to 2 SD and does not say whether the log was kept).

## Code
No code link.

## Survey CSV discrepancies
No value contradicts the paper (2 SD, 3 cells, > 1 electrode, 180–220 Hz, speed not reported). Notes:
- Min. Duration 40 / Max Duration 600 ms are the limits of the flexible sequence-scoring window, not SWR-duration limits; the notes should say so.
- Detection "SWR" / Notes "SWR" hide that the event windows are defined by a spike-sequence score, with SWR power (> 2 SD) only as a gate.

## Package mapping
Tier: D     Needs radiatum: n   Needs theta: n   Needs sleep scoring: n
Recipe (approximation of the SWR gate only; the sequence-scoring windows are outside the package):
```python
import numpy as np
from ripple_detection import filter_ripple_band, get_envelope, normalize_signal, threshold_by_zscore
# lfps: (n_time, n_tetrodes), all hippocampal tetrodes
amp = get_envelope(filter_ripple_band(lfps, fs, band=(180.0, 220.0))).mean(axis=1)
z = normalize_signal(amp)                         # "mean for the session"; Jackson 2006 used log(amp)
swr = np.asarray(threshold_by_zscore(z, time, minimum_duration=0.0, zscore_threshold=2.0))
# user code (outside the package): flexible-window pairwise sequence score; keep windows that
# overlap an `swr` interval (require_overlap), with >= 3 active cells, at a reward site while paused
```
Remaining deviations:
- The event itself is a window grown by a spike-order sequence score (40–600 ms, 10 ms steps); no detector produces it. This is content-defined, like a decoding-defined event.
- How a window passes the SWR-power gate (peak vs any sample vs mean above 2 SD) and whether power was log-transformed are not stated.
- "Paused at reward sites" has no stated speed or location rule.
- Filter design not stated (package: equiripple FIR at 180–220 Hz).
Smallest package addition (if C): n/a (D). The SWR gate alone is B as above (`require_overlap` of externally scored windows with the SWR intervals).
