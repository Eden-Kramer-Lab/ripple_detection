# Mou 2022 — Observational learning promotes hippocampal remote awake replay toward future reward locations
Source: PMC author manuscript PMC8897267 (NIHMS1764670), full-text JATS XML fetched with NCBI E-utilities (`efetch.fcgi?db=pmc&id=8897267`) and converted to the extracted text There was no PDF in Dropbox/Papers or Zotero. The ScienceDirect and cell.com pages returned 403, and pmc.ncbi.nlm.nih.gov served a reCAPTCHA. Title verified: yes (Mou, Pokhrel, Suresh & Ji, Neuron 110:891–902, doi 10.1016/j.neuron.2021.12.005).
Trigger: MUA

September 25, 2026 source update: a PDF is now present in the local Zotero library.
Its STAR Methods (PDF p. 18, "LFP analysis and population burst events" and
"Replay identification and shuffling") independently confirm the 10 ms bins,
Gaussian SD of two bins (20 ms), 0.35/0.15 normalized thresholds, 30 ms merging,
four active template cells, and 20 ms decoding windows advanced by 10 ms.

## Method as implemented

Detection (population burst events, PBEs):
- "We detected PBEs by the multiunit activity (MUA) as in previous studies (Diba and Buzsaki, 2007; Wu et al., 2017). In each session, all putative spikes recorded by all tetrodes in the CA1 were binned and counted in each 10 ms time bin. Spike counts in each bin were smoothed by a Gaussian kernel with a σ of two bins and normalized from 0 to 1. A PBE was defined as a time period within which the normalized peak MUA spike counts exceeded a threshold of 0.35, with its start and end times detected by crossing a threshold of 0.15. Adjacent events with gap < 30 ms were combined." (STAR Methods, "LFP analysis and population burst events (PBEs)").
- Signal: all putative spikes, unsorted. "A 60 μV threshold was set for spike detection" (STAR Methods, "Data acquisition"). Tetrodes are in the CA1 pyramidal layer ("Recording procedure").
- Smoothing: Gaussian σ = 2 bins = 20 ms, on 10 ms bins.
- Normalization: "normalized from 0 to 1", i.e. a range normalization, not a z-score. Whether this is min–max or division by the maximum is not stated, but for a spike-count trace with minimum ≈ 0 the two nearly coincide. The period is "In each session".
- Thresholds: peak "exceeded" 0.35 (strict), and bounds are the crossings of 0.15.
- Merging: "Adjacent events with gap < 30 ms were combined."
- Minimum and maximum duration: not stated.
- Speed: none stated for detection. The analysed PBEs are those "within the water consumption periods in the observation box" (STAR Methods, "Replay identification and shuffling": "a PBE rate (number of PBEs per s within the water consumption periods in the observation box)"). Results: "Ripples and PBEs primarily took place during reward consumption when the OB was stationary with low speeds". The restriction to reward-consumption periods is an analysis window, not a speed threshold. How consumption periods are defined is not stated beyond the task description.
- Brain state: none (awake task).
- Participation (analysis, not detection): "For each template, we defined those PBEs with at least 4 active template cells as candidate events." (STAR Methods, "Replay identification and shuffling").
- LFP: ripple power (PSD 100–250 Hz, compared over 130–220 Hz) was used only to compare conditions, not for detection. LFPs were "not always available because of the noise produced by animals making nosepokes".
- Separately, "stopping periods (velocity < 5 cm/s for > 3 s)" were excluded from place-field rate curves ("Firing rate curves and template construction"). That is analysis only.

## Inherited from
The paper cites Diba & Buzsáki 2007 and Wu et al. 2017 "as in previous studies", but it states a different, complete rule. Wu 2017 (manifest 33_Wu_2017) uses unsmoothed 10 ms counts, peak ≥ mean + 4 SD, bounds at the mean and 50–400 ms, with no range normalization and no merge. So "as in previous studies" refers to using MUA, not to the thresholds. Nothing needed to be followed to reproduce it.

## Code
"The main MATLAB codes for the analysis are publicly available in GitHub (https://github.com/DaoyunJiLab/DM2021.git; DOI: 10.5281/zenodo.5758889)." I downloaded the repository (commit c7327f8) and grepped for the MUA/PBE detection (0.35, 0.15, MUA, burst). The PBE detection is not in the repo. It has the DataManager GUI, place-field and decoding code, and a menu entry naming `DataManager_V1CA1_FindMUAlevel_Callback`, whose file is absent. So the code does not settle the min–max vs divide-by-max question.

## Survey CSV discrepancies
Row 11:
- Min. Cells 4: this is the candidate-event criterion for decoding (analysis), not a PBE criterion.
- Everything else matches: MUA smooth 20 ms (σ = 2 × 10 ms bins), Combine 30 ms, Detection Notes "Normalized MUA to 1 and used 0.35 as a threshold, extend to 0.15", no durations, no speed.
- Not recorded: unsorted spikes (> 60 µV), per-session range normalization, and the restriction of analysed PBEs to reward-consumption periods.
Count: 0 substantive discrepancies; 1 analysis-level field (min cells).

## Package mapping
Tier: B     Needs radiatum: n   Needs theta: n   Needs sleep scoring: n
Recipe:
```python
import numpy as np
from ripple_detection import get_multiunit_population_firing_rate, merge_close_events, require_overlap
from ripple_detection.core import extend_threshold_to_mean

# counts: (n_bins, n_tetrodes) counts per 10 ms bin of all putative spikes on CA1 tetrodes, one session
fs = 100
rate = get_multiunit_population_firing_rate(counts, fs, smoothing_sigma=0.020)   # sigma = 2 bins
norm = (rate - rate.min()) / (rate.max() - rate.min())                           # "normalized from 0 to 1"
pbe = np.asarray(extend_threshold_to_mean(norm > 0.15, norm > 0.35, bin_time, minimum_duration=0.0))
pbe = merge_close_events(pbe, close_event_threshold=0.030)
pbe_consumption = require_overlap(pbe, consumption_intervals)                  # analysis window (user-defined)
candidates = ...  # analysis: >= 4 active template cells per PBE (sorted units, user code)
```
Smoke-tested on simulate_multiunit output binned to 10 ms (it runs; 22–34 PBEs on 23 simulated bursts).
Remaining deviations:
- No detector does range normalization. normalize_signal offers only "zscore" and "median_mad", so the trace is user code. extend_threshold_to_mean then applies the two thresholds exactly, with strict ">" as in the paper.
- Min–max vs divide-by-max: not stated, and the code does not include it. The difference is small when the smoothed minimum is near 0, which it will be in any session with silent bins.
- Sensitivity: a range-normalized threshold depends on the single largest bin of the session. One artifact or one unusually large burst rescales every threshold. This is a property of the method, not of the port, but it matters when comparing sessions or conditions.
- Bounds are the first and last bins > 0.15, not interpolated crossings, which is within a bin.
- The 30 ms merge is strict (< 30 ms), which matches merge_close_events.
- gaussian_smooth truncates at 8 SD and renormalizes at the edges, while MATLAB-style smoothing may truncate differently. That is negligible.
Smallest package addition (if C): not needed for B. A `normalization_method="range"` (min–max) option plus a `bounds_threshold` on `multiunit_HSE_detector` would make it A.
