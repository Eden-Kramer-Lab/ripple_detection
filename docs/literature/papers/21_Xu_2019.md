# Xu 2019 — Assembly Responses of Hippocampal CA1 Place Cells Predict Learned Behavior in Goal-Directed Spatial Tasks on the Radial Eight-Arm Maze
Source: the extracted text (pdftotext of the Dropbox PDF, Neuron 101:119–132); title verified: yes
Trigger: MUA (HSE of sorted pyramidal cells)

## Method as implemented
Detection criteria:
- Signal: sorted (clustered) pyramidal cells, not unsorted MUA. "High synchrony periods were detected using the multi-unit activity of clustered spikes, similar to that previously described (Davidson et al., 2009). The combined activity of pyramidal cells was binned into 1ms bins and smoothed with a Gaussian kernel (SD = 15 ms), to produce a curve representing the synchronous spiking rate over time." (STAR Methods "High Synchrony Events (HSE)", p. e3)
- Threshold and bounds: "HSE detection began when this curve passed 3 SD above the mean. The envelope of the HSE was extended until the curve again reached the mean rate, either side of this crossing. The peak of the HSE represented the peak of the curve within this envelope." (p. e3). The period the mean and SD come from is not stated.
- Participation and duration: "Events containing fewer than five spikes, four cells or with less than 10% of the population of neurons active were rejected, as were events shorter than 75ms or greater than 750 ms." (p. e3). These apply to the envelope extended to the mean.
- Onset: "The beginning of the HSE was then adjusted to the time of the first spike." (p. e3)
- Speed, theta, sleep and merging: none stated for HSE detection. The 5 cm/s speed filter appears only for rate maps (p. e2). HSEs are analysed by location (center vs goal), which is analysis.

Analysis restrictions (not detection):
- "The HSE was then subdivided into 20ms windows, with a 10 ms overlap, until the last window containing a spike was reached. Following this procedure, all events with less than five windows were rejected from further analysis." (p. e3)
- Replay trajectories: "minimum four time windows ... covering at least 20 bins and had an average speed larger than two bins per time window" (Replay during HSEs, p. e3).

Secondary SWR detection (analysis only: SWR–HSE cross-correlation, Fig. 6B):
- "SWR detection was performed as previously described (Csicsvari et al., 1999). Local field potentials were band-pass filtered (150–250 Hz), and a reference signal (from a channel that did not contain ripple oscillations) was subtracted to eliminate common-mode noise (such as muscle artifacts). The power (root mean square) of the filtered signal was calculated for each electrode and summed across electrodes designated as being in the CA1 pyramidal cell layer. The SWR detection threshold (7 SD above baseline) was always set in the first available sleep session, and the same threshold used throughout." (SWR Detection, p. e4). The RMS window, bounds and duration are not stated.

## Inherited from
- HSE "similar to that previously described (Davidson et al., 2009)". Davidson 2009 (Dropbox PDF, Experimental Procedures, "Candidate Replay Events") specifies: "A smoothed histogram (1 ms bins; Gaussian kernel, SD = 15 ms) was constructed of multiunit activity (MUA) including all spikes with a peak amplitude greater than 100 mV on any channel, whether or not they are part of an isolated cluster. Mean and standard deviation of MUA during STOP was calculated, and candidate replay events were defined as epochs during which MUA was higher than the mean and peak rate was at least three standard deviations above the mean. Only candidate events within 30 s of RUN were analyzed". STOP means linearized speed < 5 cm/s.
  Xu departs from Davidson in three ways. (a) Clustered pyramidal spikes, not all threshold-crossing spikes. (b) No STOP-only normalization is stated. (c) It adds participation and duration criteria. Whether Xu inherited Davidson's STOP normalization is UNKNOWN.
- The SWR detection cites Csicsvari et al. 1999, J Neurosci 19:274–287. Not retrievable here (403/CAPTCHA). The RMS window and bounds are unknown. Affects only the secondary SWR detector.

## Code
None. "Data used in this study will be made available upon request" (p. e4).

## Survey CSV discrepancies
No value discrepancies: MUA Z 3, MUA smooth 15, Min cells 4, Min 75 ms, Max 750 ms, speed N/A and combine N/A all agree.
Omissions: the notes ("MUA") leave out that the spikes are CLUSTERED pyramidal cells, the bounds at the mean, the additional ≥10%-of-population and ≥5-spike criteria, and the onset moved to the first spike. The SWR detector at 7 SD (first sleep session baseline) is analysis only and correctly not in the SWR columns.

## Package mapping
Tier: A (plus a one-line spike-count filter)     Needs radiatum: n   Needs theta: n   Needs sleep scoring: n
Recipe:
```python
from ripple_detection import multiunit_HSE_detector
# pyramidal: (n_time, n_pyramidal) spike counts of sorted pyramidal cells at 1 kHz (1 ms bins)
n_cells = pyramidal.shape[1]
hse = multiunit_HSE_detector(
    time, pyramidal, speed, 1000.0,
    zscore_threshold=3.0, minimum_duration=0.0, smoothing_sigma=0.015,
    speed_threshold=np.inf,                                   # no speed criterion stated
    minimum_active_units=max(4, int(np.ceil(0.10 * n_cells))),
    maximum_duration=0.750,
    normalization_mask=None,                                  # period not stated
)
hse = hse[hse.duration >= 0.075]
spikes_in = [pyramidal[(time >= s) & (time <= e)].sum() for s, e in zip(hse.start_time, hse.end_time)]
hse = hse[np.asarray(spikes_in) >= 5]
# onset := first spike inside the event (user code)
```
Checked: runs on simulate_session data (21 of 22 detections matched ground truth in a 60 s synthetic test; a smoke test only).
Secondary SWR (Tier A): `Roumis_ripple_detector(time, filter_ripple_band(lfp_ca1 - ref[:, None], fs), speed, fs, zscore_threshold=7, normalization_mask=first_sleep_session, speed_threshold=np.inf, minimum_duration=0.0)`. A sum of per-electrode RMS is a constant times Roumis's per-channel mean, and a z-score ignores the constant. The RMS window and the bound rule are unknown.
Remaining deviations:
- "passed 3 SD": strict ">" vs the package's ">=". Negligible on a continuous trace.
- Order of criteria: the package applies minimum_active_units before maximum_duration (`_finish_events`), and the 75 ms minimum is a post-filter. The order does not matter here because nothing is merged or dropped by proximity.
- The duration filters apply to the event extended to the mean, as in the paper, which applies them before the first-spike onset adjustment.
- Normalization period not stated (Davidson used STOP periods; pass `normalization_mask=speed < 5` if you follow Davidson).
- The package's Gaussian is truncated at 8 SD and renormalized at block edges. The paper's truncation is not stated.
Smallest package addition (if C): n/a. Optional: a `minimum_spike_count` argument and a `minimum_active_fraction` on multiunit_HSE_detector would remove the user-side filters.
