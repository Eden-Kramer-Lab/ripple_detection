# Tirole 2022 — Experience-driven rate modulation is reinstated during hippocampal replay
Source: the extracted text (pdftotext of the Dropbox PDF), plus the authors' code (see Code); title verified: yes (Tirole, Huelin Gorriz, Takigawa, Kukovska & Bendor, eLife 11:e79031)
Trigger: SWR+MUA (an MUA burst is the event; a ripple-power peak inside it is required)

## Method as implemented

Detection. Paper quotes are from Methods, "Candidate replay event detection" (p. 19). Code quotes are from `Pipeline/Extract replay and Bayesian Decoding/extract_replay_events.m` and neighbours at commit 44ecf42.
- Signal: sorted and unsorted spikes pooled. The paper says only "Candidate replay events were detected based on MUA." The code builds the histogram from single units plus the clusters rated multi-unit: `all_MUA_time= [clusters.spike_times; spike_times_MUA]` (`process_clusters.m`). Clusters rated noise are left out ("The rest of the clustered activity was classified in either multi-unit activity (MUA) or noise", Methods, "Spike detection and unit isolation", p. 18). All tetrodes are pooled (CA1 pyramidal layer).
- Smoothing. Paper: "MUA was first smoothed with a Gaussian kernel (sigma = 5 ms) and binned into 1 ms steps." Code: 1 ms histogram, then `w=gausswin(41,2); w=w./sum(w); %gaussian kernel 10 ms STD, 2 std width`, then `MUA.zscored=zscore(filtfilt(w,1,histcounts(...)))`. gausswin(41, 2) has nominal Gaussian SD (41−1)/(2·2) = 10 samples = 10 ms. After normalization, the finite 41-point kernel has SD 8.898 ms; filtfilt applies it twice, giving effective SD 12.583 ms. The untruncated approximation would be 14.142 ms. **Text and code disagree (5 ms versus this two-pass kernel).**
- Normalization: `zscore` over the whole recording from `min(Time)` to `max(Time)`, so PRE, RUN and POST are all included. The paper does not say.
- Threshold and bounds. Paper: "Only MUA bursts with a maximum duration of 300 ms and z-scored activity over 3 were selected." Code: supra-threshold samples are `mua_zscore>=3`. A new burst starts wherever consecutive supra-threshold samples are ≥10 ms apart: `find(diff([0 time(mua_idx)])>=0.01)`. From the burst's first supra-threshold sample the code searches ±300 samples (300 ms, `max_search_length=300`). Onset is the last sample with z < 0 before it and offset is the first sample with z < 0 after it. If none is found within 300 ms, the bound criterion is relaxed to z ≤ 0.25, then to z ≤ 0.5. After that the event is flagged `error` and the search-window edge is used. So "maximum duration of 300 ms" means this ±300 ms search window, not a filter on duration.
- Minimum duration: "discarded if ... their duration was below 100 ms". In code, `replay.duration < 0.1` removal runs on onset-to-offset **before** merging.
- Merge: "Events detected within 50 ms were combined." In code: `replay.onset(2:end)-replay.offset(1:end-1)<0.05`, repeated until nothing merges. It runs after the minimum-duration cut and before the speed, participation and ripple criteria.
- Maximum duration: the paper says "or over 750 ms". **The code only counts these and keeps them**: `% check for long events (more than 1 second) but leave them` / `number_of_long_events=length(long_events)`. No other script enforces 0.75 s (grep of both repositories).
- Speed: "the animal's speed was required to be less than 5 cm/s". Code: `speed = interp1(position.t,position.v_cm,[onset:.01:offset],'nearest'); replay.speed(i) = median(speed)`, with events removed when `replay.speed>5`. This is a median over the event, sampled every 10 ms.
- Participation: "discarded if their sequence had less than five different units active". Code: `replay.neuron_count<5` removed. `neuron_count` counts unique `place_fields.good_place_cells` (cells with a good place field on at least one track) with ≥1 spike in [onset, offset]. The denominator is therefore place cells, an absolute count of 5, not the units that make up the MUA trace.
- Ripple (required, AND): "the ripple-band filtered LFP signal was smoothed with a 0.1 s moving average filter and a threshold over 3 was set for the z-scored ripple power, and used as an additional criterion for candidate replay events." Code, in `extract_CSC.m`:
  - One channel, the one with the highest ripple-band PSD power (`bestCSC_ripple`, `determine_best_channel.m`). Methods, "LFP analysis" (p. 18): "identify the channels with higher power for theta (4–12 Hz) and ripple (125–300 Hz)".
  - Downsampled to 1 kHz (`CSC(jj).SR = 1000`), then `fir1` band-pass 125–300 Hz of order round(6·1000/175) = 34, applied with `filtfilt`.
  - Envelope and smoothing: `CSC(jj).ripple_zscore = zscore(smooth(abs(hilbert(CSC(jj).ripple)),15)')`. That is the Hilbert amplitude, then a **15-sample (15 ms) moving average**, then a z-score over the whole recording. **Text says 0.1 s.**
  - Test: `replay.ripple_peak(i) = max(ripple_zscore(time>onset & time<offset))`, then `find(replay.ripple_peak >= 3)` in `Pipeline/Replay scoring and shuffling/number_of_significant_replays.m`. So the peak ripple z strictly inside the MUA event must be ≥ 3.
  - The Results confirm the ripple is a requirement. A robustness control is described as "not requiring a threshold for ripple power for candidate replay events (Figure 1—figure supplement 6 ...)" (Results, p. 6).
- Brain state: none for detection.

Later analysis steps (not detection):
- Decoding split: "candidate replay events were split in half ... the minimum MUA activity in the middle third of the candidate replay event was used to determine a natural midpoint". This is decoding only.
- Replay classification: awake versus rest ("Replay events detected during sleep or rest periods within the rest pot were classified as rest replay events"). This is analysis, not detection.
- Significance: weighted correlation against three shuffles, with p < 0.05 for whole events and p < 0.025 for halves.

## Inherited from
The code header says "based on Davidson, Kloosterman, and Wilson(2009,Neuron) criteria. - events detected by MUA only - zscore of 3 minimum, and edges detected by zscore of 0". The paper cites nothing for detection. The code settles every detail, so I did not follow the Davidson citation.

## Code
- https://github.com/bendor-lab/Elife_Tirole_Huelin_Gorriz_2022, cloned at commit 44ecf4275c2a7ca33dda6b67f11b4e854c3123e9. This is the same revision as the paper's Software Heritage archive `swh:1:rev:44ecf42...`.
- Detection: `Pipeline/Extract replay and Bayesian Decoding/extract_replay_events.m`
- MUA trace: `Pipeline/Extract spikes, waveforms and dropped samples/process_clusters.m`
- Ripple trace: `Pipeline/Extract CSC/extract_CSC.m`
- Ripple threshold: `Pipeline/Replay scoring and shuffling/number_of_significant_replays.m`
- Parameters: `Pipeline/list_of_parameters.m` (`min_zscore=0`, `max_zscore=3`, `max_search_length=300`, `min_event_duration=0.1`, `max_event_duration=0.750`, `MUA_filter_length=41`, `MUA_filter_alpha=2`, `ripple_filter=[125 300]`)

## Survey CSV discrepancies
Row 6.
- Detection Notes: CSV says "MUA". The main-analysis events also require peak ripple z ≥ 3 inside the MUA event (see the quote above). The "Detection" column ("SWR, MUA") is right; the notes should say "MUA burst AND ripple z ≥ 3 inside it".
- The CSV matches the paper text on every value (3, 3, 5 cm/s, 5 ms, 100 ms, 5 cells, 1 electrode, 125–300 Hz, 100 ms, 750 ms, 50 ms). The text itself differs from the code in three places:
  - MUA smoothing: 5 ms in the text, versus a 10 ms SD Gaussian applied with filtfilt (12.583 ms finite-kernel effective SD) in the code.
  - SWR smoothing: 100 ms in the text, versus a 15 ms moving average in the code.
  - Max Duration: 750 ms in the text, not enforced in the code.
- Min. Cells "5" is right but the cells are place cells with a field on at least one track, not any unit.

## Package mapping
Tier: B     Needs radiatum: n   Needs theta: n   Needs sleep scoring: n
Recipe (smoke-tested on `simulate_session` data at 1 kHz):
```python
import numpy as np
from ripple_detection import multiunit_HSE_detector, filter_ripple_band, get_envelope, normalize_signal
from ripple_detection.core import merge_close_events, exclude_movement_by_majority

fs = 1000.0
# all_spikes: (n_time, n_units) 1 ms counts, single units + multi-unit clusters, all tetrodes
hse = multiunit_HSE_detector(
    time, all_spikes, speed, fs,
    zscore_threshold=3.0, minimum_duration=0.0,   # one sample >= 3, bounds at z = 0
    smoothing_sigma=0.005,                        # text; 0.012583 matches code variance, not its kernel shape
    speed_threshold=np.inf, minimum_active_units=0)  # criteria applied after merging, below
ev = hse.loc[hse.duration >= 0.100, ["start_time", "end_time"]].to_numpy()  # >= 100 ms BEFORE merging
ev = merge_close_events(ev, 0.050)                                          # gap < 50 ms joins
ev = exclude_movement_by_majority(ev, speed, time, speed_threshold=5.0)     # ~ median speed <= 5
ev = ev[n_active_units(place_cell_spikes, time, ev) >= 5]                   # user code, ~4 lines
env = get_envelope(filter_ripple_band(best_ripple_lfp, fs, band=(125, 300)))
ripple_z = normalize_signal(np.convolve(env, np.ones(15) / 15, mode="same"))  # code: 15 ms boxcar
ev = ev[peak_within(ripple_z, time, ev) >= 3.0]   # user code: max over time strictly inside (start, end)
# text only (code does not enforce): ev = ev[(ev[:, 1] - ev[:, 0]) <= 0.750]
```
An A-tier approximation for the ripple step: `Karlsson_ripple_detector` on the single channel with `zscore_threshold=3, minimum_duration=0.0, smoothing_sigma=0.0043` (the SD of a 15 ms boxcar), `speed_threshold=np.inf`, then `require_overlap(mua_events, ripple_events)`. This is looser, because a ripple whose peak lies outside the MUA event but whose tail extended to the mean overlaps it also passes.
Remaining deviations:
- Bounds: the package extends to the run with z ≥ 0 with no length cap. Tirole searches only ±300 ms from the burst's first supra-threshold sample, relaxes the bound to z ≤ 0.25 then z ≤ 0.5, and otherwise cuts at the window edge. Tirole bounds also sit at the first sample with z < 0, one sample wider on each side.
- Smoothing kernel: the package applies a single Gaussian. Tirole's code uses gausswin(41,2) via filtfilt (12.583 ms effective SD; nominal SD 10 ms, truncated at ±20 ms per pass); the text says 5 ms.
- Ripple trace: 125–300 Hz `fir1` of order 34 versus the package's remez FIR; a 15-sample moving average (code) or 0.1 s (text) versus Gaussian smoothing. The package has no boxcar option, so the recipe does it in numpy.
- Ripple criterion: a peak strictly inside the MUA event. No public helper exists (user code); `require_overlap` against ripple events is only an approximation.
- Participation counts place cells, a different unit set from the MUA trace. `minimum_active_units` counts the trace's own columns, so this needs user code.
- Speed: the median of 10 ms-sampled speed versus the package's "at least half of samples ≤ 5". They can disagree only on a 50/50 tie.
- Order: min duration → merge → speed/participation/ripple. The recipe reproduces it by disabling the detector's own criteria. The detector's built-in order (criteria before the proximity rule, which drops rather than merges) differs.
- Burst splitting at ≥10 ms supra-threshold gaps produces duplicate events that the 50 ms merge then joins. The effect is equivalent to one event per above-mean run.
- Normalization: whole recording (all epochs) for both MUA and ripple. The package default (all samples passed in) matches if the whole session is passed.
Smallest package addition (if C): n/a (B). Two small public helpers would make it nearly A: `count_active_units(spikes, time, event_times)` for any unit subset and any intervals, and `peak_within(trace, time, event_times)` (or `require_peak(events, trace, threshold)`). The ±300 ms capped search with adaptive bounds is idiosyncratic and not worth adding.

## Independent parameter recheck — September 25, 2026

The current CSV supersedes the historical discrepancy list below. Independent checks and
source limitations are indexed in [the source recheck](../source_recheck.md) and
[the complete field-status ledger](../parameter_verification_2026-09-25.csv).

- **Sig. Thresh.**: `Whole event p<0.05; split half p<0.025 (all three)`. Tirole release 44ecf42, number_of_significant_replays.m.

Current decoding notes: Whole-event replay must pass all three shuffle tests at p<0.05; a split half must pass all three at p<0.025. Shuffles are 1000 per method.
