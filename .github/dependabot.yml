version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    cooldown:
      default-days: 7  # let a new action release age a week before proposing it
    groups:
      actions:
        patterns: ["*"]
